# Drill 01 — Debugging (Django App)

**App:** `drill-app-django`
**Namespace:** `incident-mgmt`
**Time limit:** 15 minutes

---

## Setup

The fault has already been injected. Start debugging.

Open your debug terminal, navigate to the app directory, and start a session log:

```bash
cd /workspaces/platform-drill-fastapi-postgres/codespace/drill-app-django && script -q -a ./session.log
```

---

## Symptom

> "The incident API was working earlier but external requests aren't getting through anymore. Can you investigate?"
